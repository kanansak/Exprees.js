# Monitering Energy Platform 

เป็น API ที่ใช้ในการรับ-ส่งข้อมูล เพื่อใช้ในการแสดง เพิ่ม แก้ไข ลบ ข้อมูล จาก Database


npm install express
npm install express mysql
npm install -g nodemon
npm install express cors //การกำหนด CORS (Cross-Origin Resource Sharing) ในเซิร์ฟเวอร์ต่างกันไปตามเว็บเซิร์ฟเวอร์ที่คุณใช้ ต่อไปนี้เป็นตัวอย่างการกำหนด CORS ในเซิร์ฟเวอร์ Node.js โดยใช้ Express.js:
npm install --save express-oauth2-jwt-bearer
